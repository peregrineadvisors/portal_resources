{
    "name": "peregrineportal",
    "version": "0.0.1",
    "description": "Classes used for parsing data used in our website portal.",
    "main": "",
    "author": "Josh Cardenzana",
    "license": "ISC",
    "scripts": {
        "compile": "tsc -b",
        "build": "tsc -b",
        "compile:test": "npm run compile && cd test && npm run compile && cd ../;",
        "watch": "tsc -b -w"
    },
    "devDependencies": {
        "@types/jquery": "^3.3.1",
        "@types/node": "^12.12.2",
        "tslint": "^6.1.3",
        "typescript": "^4.0.3"
    },
    "dependencies": {
        "@types/node": "^12.12.2",
        "tslint": "^6.1.3",
        "typescript": "^4.0.3"
    }
}
